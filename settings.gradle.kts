pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Multiplatform_beta"
include(":multiplatform_Beta")
include(":shared")